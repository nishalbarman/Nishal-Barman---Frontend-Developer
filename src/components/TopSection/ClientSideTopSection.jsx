"use client";

import React from "react";
import CustomButton from "../CustomButton/CustomButton";

function ClientSideTopSection() {
  return <CustomButton title={"Get Details"} onClick={() => {}} />;
}

export default ClientSideTopSection;
